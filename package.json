{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-run-all2": "5.0.2"
  },
  "scripts": {
    "bootstrap": "npm install;npx lerna bootstrap",
    "start": "run-p --print-label package:*",
    "package:header": "lerna run --scope header --stream dev",
    "package:shared": "lerna run --scope shared --stream dev",
    "build": "lerna run --parallel build"
  },
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "next": "10.0.6",
    "next-transpile-modules": "6.1.0",
    "postcss": "^8.2.4",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "tailwindcss": "^2.0.2"
  }
}
